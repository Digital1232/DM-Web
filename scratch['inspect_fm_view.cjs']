const fs = require('fs');
const path = require('path');

const indexHtml = fs.readFileSync(path.join(__dirname, '..', 'index.html'), 'utf8');

let idx = indexHtml.indexOf('view-files-manager-panel');
if (idx !== -1) {
  const lineNo = indexHtml.substring(0, idx).split('\n').length;
  console.log(`=== view-files-manager-panel at line ${lineNo} ===`);
  console.log(indexHtml.substring(idx - 100, idx + 2500));
}

// Check JS for files-manager logic
const scriptJs = fs.readFileSync(path.join(__dirname, '..', 'script.js'), 'utf8');
idx = scriptJs.indexOf('files-manager');
if (idx !== -1) {
  const lineNo = scriptJs.substring(0, idx).split('\n').length;
  console.log(`=== script.js files-manager at line ${lineNo} ===`);
  console.log(scriptJs.substring(idx - 100, idx + 1000));
}
